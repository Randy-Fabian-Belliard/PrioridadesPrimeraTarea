@page "/C_Prioridades"
@inject PrioridadesBLL prioridadesBLL;

<table class="table table-bordered">
    <thead>
        <tr>
            <th>Prioridad ID</th>
            <th>Descripci�n</th>
            <th>D�as de compromiso</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in prioridades)
        {
            <tr>
                <td>@item.PrioridadId</td>
                <td>@item.Descripcion</td>
                <td>@item.DiasCompromiso</td>
            </tr>
        }
    </tbody>
</table>

@code {
    public List<Prioridades> prioridades { get; set; } = new List<Prioridades>();

    protected override async Task OnInitializedAsync()
    {  
        prioridades = await prioridadesBLL.Listar(p => true);
    }
}
